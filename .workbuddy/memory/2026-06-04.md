## 2026-06-04 部署

### 第一次提交（78a7a6f）
- 包含 normalizeCategory、cache.ts、categories 更新、resolveCategoryBySlug 等全部修复
- SSH 推送被沙箱阻断，后用户手动 push

### Cloudflare Pages 构建失败
- `Navbar.tsx` 使用了 `useState` 但缺少 `"use client"` 指令
- 本地 ts 检查不报错，但 Cloudflare 生产构建严格模式会报

### 修复（8975811）
- Navbar.tsx 头部添加 `"use client"` 指令
- 提交 `fix: Navbar 缺少 'use client' 导致 Cloudflare 构建失败`
- 已成功 push 到 GitHub，Cloudflare Pages 自动重新构建部署

### 首页重构（06b940a）
用户反馈原来分类卡片+跳转子页面的交互不对，要求：
- 顶部展示分类Tab，数据来自R2
- 默认展示第一个分类的音频
- 切换分类直接切换数据，不跳页

改动 `src/app/page.tsx`：
- 水平滚动分类Tab：来自 `/api/audio-list` 的 categories，按数量排序，"其他"沉底
- 默认选中首分类 → 自动 fetch `/api/audio-list?category=xxx`
- 切换Tab → `activeSlug` 变化触发 useEffect 重新拉取对应分类文件
- 双栏 AudioPlayer 列表 + "加载更多"分页
- 骨架屏替换了旧的分类卡片网格
- 移除了 UploadZone 上传模块（后续可加回）

已 push，Cloudflare Pages 自动部署中

### 分类Tab平铺（7514bc7）
- 分类Tab从水平滚动改为 flex-wrap 平铺换行

### Logo+布局+子页优化（后续提交）
- Navbar Logo w-10→w-12，文字 text-xl→text-2xl
- 音频列表 grid-cols-2→grid-cols-4 响应式
- /custom 和 /music 页面全面重写，统一暗色风格

### 整体色调优化（0316afe）
用户反馈网站太暗。系统性提亮：

**CyberBackground 增强：**
- 光晕不透明度 30%→40/50%，新增第四个 violet blob
- 新增顶部暖光渐变(amber-500/20)
- 霓虹线条 opacity 60%→80%
- 网格 opacity 0.20→0.25
- 新增3个浮动发光粒子

**色彩体系升级：**
- 全局基底: #0a0a0a→#0f0f1a（深蓝紫调，更温暖）
- 页面渐变: gray-900→[#0f0f1a→#13132b→#0f0f1a]
- 卡片背景: slate-800→[#1a1a30]（统一玻璃态色彩） 	
- 边框: 不透明度 10/20%→20/25%
- 文字: text-slate-500→400, text-slate-600→500（全站提一级）

**修改文件：** CyberBackground.tsx, globals.css, page.tsx, Navbar.tsx, AudioPlayer.tsx, custom/page.tsx, music/page.tsx

### 定制页重构 — 移除报价，突出音色&内容（2c191bf）
用户要求去掉定价，突出音色定制和内容定制两大板块：
- 移除基础/高级套餐定价卡片（¥99/¥199）
- 新增「音色定制」：6种风格（磁性男声/甜美女声/科技AI/游戏角色/搞笑整活/影视动漫）+ 标签 + 卡片 pink 配色
- 新增「内容定制」：6大场景（锁车解锁/迎宾语/驾驶提示/提醒播报/节日彩蛋/自定义文案）+ 示例文案 + 卡片 purple 配色
- 流程更新为：选择音色 → 定制内容 → 专业制作 → 交付安装
- 底部联系模块保留

### 音乐页 + 定制页优化（3926ce9）

**音乐页大改：**
- 移除歌单列表，改为单一商品页
- 展示「影子猩球 2026新款 车载U盘」商品卡片
- 价格 ¥63.10，链接 https://u.jd.com/71zuWat
- 双 CTA 按钮 + 4 个产品亮点（无损音质/即插即用/多车兼容/京东自营）
- cyan 配色体系

**定制页精简：**
- 音色定制 + 内容定制 从独立大板块改为 md 双栏紧凑布局
- 6+6 卡片改为横向排列，去除冗余描述和示例文案
- 流程从卡片式改为简洁编号式
- 联系模块优化：微信图标 + "开启你的专属定制" 对话式标题，微信卡片内嵌 SVG 图标

### 分类从R2直接派生（d80186d, 3737259）
- `getCategories()` 改为从 R2 文件数据派生分类，不再预填 config 中空分类
- 用户要求分类名保持 R2 原始名称，config 仅提供 URL slug

### 网站色彩重构 — 暗色极简主题（b61dd01）
- 用户觉得赛博朋克风太重（粉/紫/青霓虹、网格、扫描线、粒子）
- 新调色板：底色 `#08080d`，卡片 `bg-white/[0.03]`，边框 `white/5`，主色 `amber-400`
- CyberBackground 简化为两个微弱光斑
- 改动 9 个文件，全局去粉/紫/青，统一为暖金单色
